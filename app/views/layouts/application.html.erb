<!DOCTYPE html>
<html>
  <head>
    <title>Ecommerce</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <script src='https://api.mapbox.com/mapbox.js/v3.3.1/mapbox.js'></script>
    <link href='https://api.mapbox.com/mapbox.js/v3.3.1/mapbox.css' rel='stylesheet' />
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.0.0-alpha3/dist/js/bootstrap.bundle.min.js" integrity="sha384-popRpmFF9JQgExhfw5tZT4I9/CI5e2QcuUZPOVXb1m7qUmeR2b50u+YFEYe1wgzy" crossorigin="anonymous"></script>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.0-alpha3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-CuOF+2SnTUfTwSZjCXf01h7uYhfOBuxIhGKPbfEJ3+FqH/s6cIFN9bGr1HmAg4fQ" crossorigin="anonymous">
    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>
  <style>

  body {
      background-color: #F0F0F0;
    }

    /* Remove the navbar's default margin-bottom and rounded borders */
    .navbar {
      margin-bottom: 0;
      border-radius: 0;
    }

    .spaceq{
      height: 40px;
    }

    .centerr {
      display: flex;
      justify-content: center;
    }

    .style_image {
      width: 200px;
      height: 300px;
      border: 1px solid #000;
    }

    .small_image {
      margin: auto;
      width: 80px;
      height: 120px;
    }

    /* Set height of the grid so .sidenav can be 100% (adjust as needed) */
    .row.content {height: auto;
      min-height: 770px;
      background-color: white;
    }
    .card {
      width: 200px;
      height: 210px;
      border: 1px solid #000;
      padding: 15px;
    }

    .card-title, .card-text {
      margin: auto;
    }

    /* Set black background color, white text and some padding */
    footer {
      background-color: #555;
      color: white;
      padding: 15px;
    }


  </style>

  </head>
    <body>
      <div class="container">
        <nav class="navbar navbar-expand-sm navbar-inverse">
          <div class="container-fluid">
            <div class="navbar-header">
              <h4 class="navbar-brand">Fragrance Boutique</h4>
            </div>
            <ul class="nav navbar-nav">
              <li class="active"><%= link_to 'Home', root_path %></li>
              <li class="nav-item"><%= link_to 'Fragrances', '/product/index' %></li>
              <li class="nav-item"><%= link_to 'Brands', '/brand/index' %></li>
              <li class="nav-item"><%= link_to 'Categories', '/category/index' %></li>
              <li class="nav-item"><%= link_to 'Fragrance Family', '/fragrance_family/index' %></li>
              <li class="nav-item"><%= link_to 'My Cart', '/cart/index' %></li>
              <% if logged_in? %>
                <li class="nav-item"><%= button_to "Logout", '/destroy', method: :get%></li>
                <li class="nav-item">You are Logged In, <%= current_customer.email %></li>
              <% else %>
                <li class="nav-item"><%= button_to "Login", '/login', method: :get%></li>
                <li class="nav-item"><%= button_to "Sign Up", '/customer/new', method: :get%></li>
              <%end%>
            </ul>
          </div>
        </nav>
        <div class="container-fluid">
          <div class="row content p-3 my-3 border">
            <%= yield %>
          </div>
        </div>
        <footer class="container-fluid text-center">
          <p>Footer Text</p>
        </footer>
      </div>

    </body>


</html>