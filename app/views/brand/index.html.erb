<h1>Brand#index</h1>
<% if @brands.empty?%>
  <p>There no products yet</p>
<% else %>
<ul class="list-group"></ul>

  <% @brands.each do |brand| %>
    <li class="list-group-item"><%= link_to brand.name, brand%></li>
  <% end %>
</ul>
<% end %>
